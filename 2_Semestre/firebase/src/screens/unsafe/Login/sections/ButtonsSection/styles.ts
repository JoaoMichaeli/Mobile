import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dividerContainer: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    gap: 20,
    paddingVertical: 25
  },
  divider: {
    flex: 1,
    height: 1,
  },
})